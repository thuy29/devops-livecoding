name: Deploy with Ansible

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: SSH and deploy with Ansible
        run: |
          ansible-playbook -i inventories/setup.yml playbook.yml
